# AI Helper v2 - Grandmaster AI Coach Dependencies
# Version pinning for production stability and reproducible builds

# Core ML and Data Science Libraries
lightgbm==4.3.0              # Gradient boosting framework for ML models
scikit-learn==1.4.2          # Machine learning algorithms and utilities
pandas==2.2.2                # Data manipulation and analysis
numpy==1.26.4                # Numerical computing foundation

# System Integration Libraries
pywin32==306; sys_platform == 'win32'  # Windows API integration (Windows only)
pyautogui==0.9.54            # GUI automation and screen interaction

# Optional Dependencies with Graceful Fallback
# These can be missing without breaking core functionality
psutil==5.9.8                # System resource monitoring (optional)
pydantic==2.6.4              # Data validation (optional, manual validation fallback)

# Performance and Caching
lru-dict==1.3.0              # Efficient LRU cache implementation
pympler==0.9                 # Memory profiling and leak detection

# Configuration and Serialization
pyyaml==6.0.1                # YAML configuration file support
jsonschema==4.21.1           # JSON schema validation

# Threading and Concurrency
threading-timer==0.1.0       # Enhanced timer utilities
queue-manager==1.0.0         # Enhanced queue management (if available)

# Security and Hashing
cryptography==42.0.5         # Cryptographic operations for security
hashlib-compat==1.0.1        # Hash compatibility utilities (if available)

# Development and Testing (optional for production)
pytest==8.1.1                # Testing framework
pytest-cov==4.0.0            # Coverage reporting
memory-profiler==0.60.0      # Memory usage profiling

# Documentation Generation (optional)
sphinx==7.2.6                # Documentation generation
sphinx-rtd-theme==2.0.0      # Read the Docs theme

# Performance Monitoring
py-spy==0.3.14               # Python profiler (optional)
line-profiler==4.1.1         # Line-by-line profiling (optional)